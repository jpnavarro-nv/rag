# Cluster: Gaia (CENPES/Petrobras)
# Hardware: 4x A100 80GB per node
# Driver: R550, CUDA 12.9 (via forward compat)

NIM_BASE_DIR="${NIM_BASE_DIR:-/gaia/finetune-llm/nims/runtime}"
SLURM_PARTITION="gpu"
SLURM_ACCOUNT="llm-tic"
SLURM_GPUS_PER_NODE=4
SLURM_TIME="08:00:00"
CLUSTER_GPU="A100"
